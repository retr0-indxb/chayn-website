# Website Development Project Log (Portfolio Evidence)

| Date + Steps | Explanation | Difficulties | Evidence |
| :--- | :--- | :--- | :--- |
| **30/01/26** <br> Steps: **1-2** | This initial stage involved setting up my development environment. I installed the Antigravity IDE to serve as my primary code editor and agentic assistant. This was a crucial first step to ensure I had the necessary tools to begin building the project efficiently. | The installation process was straightforward, but I had to ensure that the IDE had the correct permissions to access my file system and that the necessary extensions were enabled for HTML and CSS development. | |
| **30/01/26** <br> Steps: **3-6** | I then proceeded to set up the project structure. I created a dedicated root folder named `chayn_cc_website` to keep all my files organized. Within this, I created an `assets` subfolder to store images like the hero background, and initialized the core `index.html` and `style.css` files. I also attempted to initialize a Git repository for version control. | I realized that organizing my files correctly from the start was essential. Initially, I was unsure where to place my images relative to the CSS file, which could have led to broken links later on. Additionally, I encountered issues with the local Git installation not being recognized by my terminal, which forced me to manage version control manually for the time being. | |
| **02/02/26** <br> Steps: **7-10** | I focused on building the structure of the **Homepage**. I implemented the semantic HTML5 layout using `<header>`, `<main>`, and `<footer>` tags. I imported the 'Inter' font from Google Fonts and the FontAwesome icon library to give the site a modern look. I then structured the navigation bar with a logo and links, and created the main Hero section to serve as the visual centerpiece. | One major difficulty I faced was that the background image for the Hero section was not displaying. After some debugging, I realized I had defined the styles in `style.css` but forgot to actually link the stylesheet to my `index.html` file using the `<link>` tag. Once I added this, the image appeared correctly. I also had to carefully adjust the Flexbox properties in the navbar to ensure the logo and links were spaced evenly on different screen sizes. | |
